#!/usr/bin/python3
def multiply_list_map(my_list=[], number=0):
    if list == None:
        return None
    
    if type(number) != int:
        return None
    
    return [el * number for el in my_list]
